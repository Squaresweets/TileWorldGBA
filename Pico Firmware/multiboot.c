#include "multiboot.h"
#include <string.h>
#include <stdlib.h>
#include <stdio.h>

/*https://github.com/Squaresweets/TileWorldGBA/raw/main/TileWorldGBA_mb.gba
The plan is to download the GBA file (so that it can be updated without having to rebuild the firmware) and then
multiboot it to the system*/